image:
  repository: kong/kong-gateway
  tag: "3.11"

secretVolumes:
- kong-cluster-cert

proxy:
  http:
    enabled:true
    servicePort:8000
    container:8000

admin:
  enabled: false

env:
  role: data_plane
  database: "off"
  cluster_mtls: pki
  cluster_control_plane: 1090d1e473.us.cp.konghq.com:443
  cluster_dp_labels: "type:docker-kubernetesOS"
  cluster_server_name: 1090d1e473.us.cp.konghq.com
  cluster_telemetry_endpoint: 1090d1e473.us.tp.konghq.com:443
  cluster_telemetry_server_name: 1090d1e473.us.tp.konghq.com
  cluster_cert: /etc/secrets/kong-cluster-cert/tls.crt
  cluster_cert_key: /etc/secrets/kong-cluster-cert/tls.key
  lua_ssl_trusted_certificate: system
  konnect_mode: "on"
  vitals: "off"
  nginx_worker_processes: "1"
  upstream_keepalive_max_requests: "100000"
  nginx_http_keepalive_requests: "100000"
  proxy_access_log: "off"
  dns_stale_ttl: "3600"
  router_flavor: expressions

ingressController:
  enabled: false
  installCRDs: false

resources:
  requests:
    cpu: 1
    memory: "2Gi"

manager:
  enabled: false
